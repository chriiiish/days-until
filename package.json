{
  "name": "days-until-api",
  "version": "1.0.0",
  "description": "Lightweight API to calculate days until a target date",
  "main": "src/index.js",
  "type": "module",
  "engines": {
    "node": ">=22.0.0"
  },
  "scripts": {
    "test": "echo \"No tests yet\" && exit 0",
    "deploy": "cd infrastructure && aws cloudformation deploy --template-file template.yaml --stack-name days-until-api --capabilities CAPABILITY_IAM",
    "package": "cd src && zip -r ../lambda.zip ."
  },
  "keywords": ["api", "days", "date", "calculator"],
  "author": "",
  "license": "MIT"
}
